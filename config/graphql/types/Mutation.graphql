type AuthPayload {
    token: String!
    user: User!
}

type User {
    id: ID!
    email: String!
    username: String
    name: String  # ← YE ADD KARO
}

type Mutation {
    login(email: String!, password: String!): AuthPayload!
    
    register(email: String!, password: String!, username: String!, name: String): AuthPayload!
    
    createOrUpdateTodo(id: ID, title: String, description: String, completed: Boolean, priority: Int): Todo!
    deleteTodo(id: ID!): Boolean!
    toggleTodoComplete(id: ID!): Todo!
}