type AuthPayload {
    token: String!
    user: User!
}

type User {
    id: ID!
    email: String!
    username: String
}

type Mutation {
    login(email: String!, password: String!): AuthPayload!
}